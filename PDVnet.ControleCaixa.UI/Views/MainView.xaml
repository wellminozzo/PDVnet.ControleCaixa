<Window
    x:Class="PDVnet.ControleCaixa.UI.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PDVnet.ControleCaixa.UI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainView"
    Width="800"
    Height="600"
    mc:Ignorable="d">
    <Grid Background="#A9ABAD">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Sidebar  -->
        <StackPanel
            Grid.Column="0"
            Margin="10"
            Background="#2C3E50">

            <Button
                Height="40"
                Margin="5"
                Foreground="White"
                HorizontalContentAlignment="Center"
                Command="{Binding ShowDashboardCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        FontFamily="Segoe MDL2 Assets"
                        Text="&#xE80F;"
                        Margin="0,0,8,0"
                        VerticalAlignment="Center" />
                    <TextBlock Text="Dashboard" VerticalAlignment="Center" />
                </StackPanel>
            </Button>

            <Button
                Height="40"
                Margin="5"
                Foreground="White"
                HorizontalContentAlignment="Center"
                Command="{Binding ShowTransacoesCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        FontFamily="Segoe MDL2 Assets"
                        Text="&#xE13D;"
                        Margin="0,0,8,0"
                        VerticalAlignment="Center" />
                    <TextBlock Text="Transações" VerticalAlignment="Center" />
                </StackPanel>
            </Button>

            <Button
                Height="40"
                Margin="5"
                Foreground="White"
                HorizontalContentAlignment="Center"
                Command="{Binding ShowFluxoDeCaixaCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        FontFamily="Segoe MDL2 Assets"
                        Text="&#xE806;"
                        Margin="0,0,8,0"
                        VerticalAlignment="Center" />
                    <TextBlock Text="Fluxo de Caixa" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>

        <!--  Conteúdo  -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentView}" />
    </Grid>
</Window>
